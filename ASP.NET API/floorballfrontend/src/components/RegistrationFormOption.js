import React from 'react'

const RegistrationFormOption = ({idValue, name}) => {
    return(
      <option id="textInput" value={idValue}>{name}</option>
    )
}

export default RegistrationFormOption